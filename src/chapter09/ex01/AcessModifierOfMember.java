package chapter09.ex01;

public class AcessModifierOfMember {

	public static void main(String[] args) {
		// 캡슐화 : 정보를 보호하기 위해서 클래스 이름(public, default), 생성자(public, default)
			//필드, 메소드 이름 앞에 붙는 접근 제어자
			//
		/*
		 	public		: 다른 패키지에서 접근 가능
		 	protected	: 다른 패키지에서 접근 가능 (단, 상속관계일 경우만 적용)
		 	default		: 동일한 패키지 내에서만 접근 가능, <<접근제어자가 없는 경우>> default는 명시하지 않음 (생략)
		 	private		: 동일한 파일 내에서만 접근 가능
		 */
	}

}
